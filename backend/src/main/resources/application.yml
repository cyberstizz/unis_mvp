file:
  upload-dir: uploads

spring:
  application:
    name: unis
  web:
    resources:
      static-locations: classpath:/static,classpath:/uploads
  datasource:
    url: jdbc:postgresql://db.bxpiryjdhxruzokseehh.supabase.co:5432/postgres?user=postgres&password=eXzT4BFduA._!?W
    username: postgres
    password: "eXzT4BFduA._!?W"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: never
  task:
    scheduling:
      pool:
        size: 2
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  jwt:
    secret: unis-super-secret-key-change-this-in-production-should-be-at-least-256-bits-long
    expiration: 86400000  # 24 hours

server:
  port: 8080

logging:
  level:
    org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration: DEBUG
    org.springframework.boot.autoconfigure.jdbc: DEBUG
    com.zaxxer.hikari: DEBUG
    org.springframework.jdbc.datasource: DEBUG
    org.springframework: INFO
    com.unis: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web.multipart: DEBUG

# New configuration property added here
unis:
  auto-populate-awards: true
